# Skip State Explorer

This the client-side of `skstate`.

It a React-based interface allowing to explore the contents of the disk-persisted memory-mapped state of a skip native program.

## Install dependencies

```bash
yarn install
```

## Run

Using `vite`:
```bash
yarn dev
```
